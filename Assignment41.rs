fn main() {
    // Booleans (`bool`)

    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    let is_evening = false; // Declare and initialize is_evening with false
    if is_evening {
        println!("Good evening!");
    }
}
